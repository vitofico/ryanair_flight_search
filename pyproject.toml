[project]
name = "ryanair-flight-search"
version = "0.1.0"
description = "Find one-stop connecting flights on Ryanair"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
]

[project.scripts]
ryanair-search = "ryanair_search:main"
